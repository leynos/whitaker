## Public functions must include documentation.

public_fn_must_have_docs = Public function { $function } requires a doc comment.
    .note = The function is exported and visible to downstream crates.
    .help = Add a doc comment describing the behaviour of { $function }.
