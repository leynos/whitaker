#[expect(dead_code, reason = "test fixture only")]
mod long_module {
    #[expect(dead_code, reason = "test fixture only")]
    pub fn one() {}

    #[expect(dead_code, reason = "test fixture only")]
    pub fn two() {}

    #[expect(dead_code, reason = "test fixture only")]
    pub fn three() {}

    #[expect(dead_code, reason = "test fixture only")]
    pub fn four() {}

    #[expect(dead_code, reason = "test fixture only")]
    pub fn five() {}

    #[expect(dead_code, reason = "test fixture only")]
    pub fn six() {}
}

fn main() {}
